profiles {
    standard {
        process.executor = 'local'
    }
}

params {
    // WORKFLOW PATHS
    simple_pipeline_workflow = "${projectDir}/workflows/simple_pipeline.nf"
}

includeConfig "${projectDir}/conf/${params.workflow}.config"
