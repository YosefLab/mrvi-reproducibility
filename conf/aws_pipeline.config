includeConfig "${projectDir}/conf/aws_credentials.config"

params {
    aws {
        root = "s3://largedonor"
    }
    inputs = ["${params.aws.root}/symsim_new.h5ad", "${params.aws.root}/scvi_pbmcs.h5ad", "${params.aws.root}/nucleus.h5ad"]
}
